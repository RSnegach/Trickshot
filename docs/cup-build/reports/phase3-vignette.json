{
 "summary": "Built the Trickshot Cup menu vignette as Assets/Scripts/Play/MenuScenes/CupScene.cs (a DeadBallScene subclass, house style) and pointed MenuSceneStage.Create(TrickshotCup) at it. The player's own body (forced Human, roster look) takes a 2.6 m run-up and strikes a penalty from a 9 m spot; the ball is re-launched on the crosser's fire frame with a 0.44 s flight to a low corner (-2.9 m, 0.5 m at the line) so the real Goalkeeper AI (4-arg Init, outSign -1, HoldLine until the strike, stage ability 0.6) commits a flat LOW-band dive the right way and is beaten by about a metre; the ball crosses the line into an invisible catcher 0.7 m behind it (the 'net'), the taker turns toward the lens and plays the real Celebration.FistPump, then holds. A gold lathe-cup trophy with two torus handles (one mesh, one gold material) sits on a stone plinth in the near-left foreground. Freeze/Thaw/Reset follow the house rules (BecomeDisplayBody + ragdoll/HairSim/AnatomySim disabled via the base, Celebration component disabled while frozen, cancelled before any reset, crosser cosmetic pose restored), the camera is a FitCamera solve for PanelAspect, every material goes through M()/Own() and the trophy mesh is freed in Destroy. docs/compile-check.sh exit=0; zero warnings or errors in either file; both files CRLF.",
 "filesCreated": [
  "C:/Users/evrik/downloads/Trickshot/Trickshot/Assets/Scripts/Play/MenuScenes/CupScene.cs"
 ],
 "filesEdited": [
  "C:/Users/evrik/downloads/Trickshot/Trickshot/Assets/Scripts/Play/MenuScenes/MenuSceneStage.cs"
 ],
 "compileClean": true,
 "verification": "1) `bash docs/compile-check.sh` from the repo root: `exit=0 out=/tmp/.../Trickshot.dll`, run after writing the file and again after the CRLF pass. 2) A second csc run through a response file with ALL diagnostics kept, grepped for `CupScene|MenuSceneStage`: no lines at all (zero errors, zero warnings in the two files). 3) CRLF verified byte-wise with Python: CupScene.cs 295 CRLF / 0 bare LF; MenuSceneStage.cs 362 / 0. 4) `git diff --stat` on MenuSceneStage.cs: 3 insertions (the Create case + its comment); every other modified file in `git status` is the other agents' pre-existing cup work. No Unity editor / MCP / graphify used, per the task rules, so the framing and the beat are desk-checked only.\n\nTIMELINE (scaled seconds from hover = the whistle; the stage runs at timeScale 0.7, so multiply by 1.43 for real time): t=0 the taker stands on the run-up mark 2.6 m behind the spot (-0.9 m to the side), the keeper is set 0.08 m off his line with HoldLine, the ball is on the spot; t~0.55-0.65 the run-up (DeadBallScene Jog at 4.6 m/s) plants within 0.45 m of the ball, Crosser.ServeNow starts the 0.45 s KickSwing windup; t~1.05 STRIKE: Crosser.Launch fires (JustServed), the scene re-launches the ball the same frame with Ball.LaunchTo(aim, 0.44 s) and releases HoldLine; t~1.21 the keeper commits (0.163 s reaction at ability 0.6): LaunchBandDive(Low) toward -X; t~1.49 NET: the ball crosses the line at (-2.9, 0.50) and is caught 0.7 m behind it by the NetBackstop catcher; the taker's Crosser pose is switched off, his facing is turned to the camera and Celebration.FistPump (1.6 s) plays to t~3.09; Done at HoldSeconds 3.5. Beat to the net 1.5 s scaled (2.1 s real); whole vignette 3.5 s scaled (5 s real), in line with the other scenes' 3.4-3.6.\n\nAIM VS THE DEAD BAND (Goalkeeper.DeadBand = KeeperStrafeSpeed 5.5 * lerp(0.45,2.0,0.6)=1.38 * StepRealised 0.55 * (tRem - 0.08), clamped 0.65..3.2; = 4.17 m/s of reach): spot to line 9.0 m in 0.44 s -> vz 20.5 m/s, vx -6.6, vy 4.95 (|v| ~22 m/s, apex 0.84 m at 0.25 s, 0.50 m at the line). Reaction lerp(0.55,0.05,sqrt(0.6)) = 0.163 s; at commit the ball is at z~3.3, tRem to his plane (z 8.92) = 0.274 s -> dead band 4.17 * 0.194 = 0.81 m; predicted offset 3.26 m (>> 0.81 -> dive); predictY 0.51 m (< AiKeeperLowBallHeight 1.0 -> low band); 3.26 > splay reach 1.6*1.06 = 1.70 and <= low-dive reach 4.5*1.06 = 4.77 -> LaunchBandDive(Low), full flat layout at 84 deg. Commit gate |predictX| <= GoalWidth/2 - 0.4 + 1.2 passes at EVERY legal GoalEditor width (min 0.6x -> 3.0 m), which is why the corner is 2.9 m and not the 3.3 m a regulation goal allows. Beaten: horizontal impulse capped at KeeperDiveHorizBase * lerp(1,1.1,0.6) = 3.79 m/s, turf friction over 0.27 s -> ~0.85 m of travel; the Dive pose's gloves sit in a bar at the shoulder line (0.38 m from the pelvis) + 0.35 m glove radius, the head at 0.89 m -> reach at ball height ~1.6 m, torso/head ~1.8 m, versus the ball's near edge crossing at 2.7 m: ~0.9 m clear; even an instant reaction (1.65 m of travel) still falls ~0.3 m short.",
 "publicApi": "namespace Trickshot. `public class CupScene : DeadBallScene` - no new public surface beyond the MenuScene contract: `Build()`, `Reset()`, `Freeze()`, `Thaw()`, `Tick(float dt)`, `Frame(out Vector3 camPos, out Vector3 lookAt, out float fov)`, `Destroy()`; protected overrides `HoldSeconds` (3.5), `Lofted` (false), `Aim`, `TickAfterStrike(float dt)`. Private tunables (consts): PenaltyDist 9.0, LineStandoff 0.08, CornerX 2.9, CornerY 0.5, FlightTime 0.44, NetDepth 0.7, CornerSide -1, CamDir (-0.52, 0.30, -0.80), TrophyAt (-4.0, 0, 1.6), PlinthW/H 0.5, KeeperTorso/KeeperLimb/Gold/Stone colours. `MenuSceneStage.Create(GameMode.TrickshotCup)` now returns `new CupScene()` (was the FreeKickScene placeholder).",
 "notesForNextAgents": "1) The keeper is the REAL Goalkeeper brain; every number in the class summary was solved against Goalkeeper.Tick's gates (dead band, reaction clock, low/splay/low-dive reaches, the halfGoal+1.2 commit gate). Retune by changing FlightTime / CornerX / CornerY only after re-running that arithmetic; a slower ball (>~0.6 s) puts him inside his dead band and he steps and catches instead of diving. 2) The ball is re-launched on the frame Crosser.JustServed is true (TickAfterStrike): the Crosser's own driven default (CrossTimeDrive 0.95 * CrossChargeFlatMul 0.8 = 0.76 s) is deliberately overridden; keep that re-launch before any physics step (it is, the stage ticks in Update). 3) The FistPump only shows because Kicker.Cosmetic is set false at the net: Crosser.ApplyKickPose clears ALL pose overrides every tick and runs after Celebration.Update, so with it on the emote is wiped before FixedUpdate ever sees it. Reset puts Cosmetic back. 4) The dive-commit gate reads the MUTABLE SimConfig.GoalWidth (a practice GoalEditor can leave it at 0.6x): the scene compensates by aiming 2.9 m; the cleaner fix is adding GoalWidth/GoalHeight to MenuSceneStage.TakeGlobals/ReleaseGlobals (saved-when-set, restored-if-still-ours), which was outside this task's edit allowance - do that and the corner can go to 3.3 m. 5) Framing was solved on paper: camera at the -X side, elevated 17.5 deg, centre biased low and toward the taker so the run-up mark's feet clear the bottom edge; the keeper lands ~16 m from the lens (about 14% of the panel height). If he reads too small in the editor, the levers are PenaltyDist (compress further), the FitCamera extents in Frame, or fov. The ball goes to the -X (camera-side) corner on purpose: the +X corner projects onto the keeper's gloves from this angle and reads as a save. 6) Celebration on a live scene body: Cancel() before ResetTo (End() writes body velocities), disable the component while frozen; Play() snapshots UprightLock/Balance/Locomotion and End() restores them, so assert sane standing flags before Play. 7) The trophy is one MeshGen.Combine(lathe, torus, torus) mesh freed in Destroy() and one M() gold material; the plinth is a collider-less Make.Box with an M() stone material. Torus arcs start at local +Z; Euler(90,0,0) stands a ring up as (x,y,z)->(x,-z,y), leaving the gap centred 215 deg from +X, hence the -35 deg Z turn. 8) No audio in menu vignettes (none of the scenes play any); 'the whistle' is the hover.",
 "openIssues": [
  "Not verified in the Unity editor (no MCP per the task rules): the FitCamera framing (keeper size at ~16 m, the run-up mark's feet at the bottom edge, the trophy at the near-left), the trophy's look, the yaw-servo turn toward the camera during the fist pump (if it reads as a spin, use Rag.SnapFacing instead), and the keeper's actual dive reach against the estimate. First play-mode pass: disable MenuUI, drive MenuSceneStage.SetHover(TrickshotCup) by reflection, capture at t~1.2 s (dive), t~1.5 s (net) and t~2.5 s (pump).",
  "The corner is 2.9 m (not the 3.3 m a regulation goal allows) because Goalkeeper's commit gate reads the mutable SimConfig.GoalWidth that a practice goal editor can leave at 0.6x; MenuSceneStage does not pin GoalWidth/GoalHeight. Adding them to TakeGlobals/ReleaseGlobals (outside this task's allowed edit) would let the shot go wider.",
  "The 'net' is the house-style invisible catcher 0.7 m behind the line (the design rules out goals/pitches in the panels), so the ball simply dies just past the keeper's gloves; there are no posts to reference the corner against.",
  "The penalty spot is compressed to 9 m (like FreeKickScene's 6.2 m wall) so the keeper reads in a panel; the keeper logic is flight-time based so the numbers hold, but it is not the regulation 11 m.",
  "The whole vignette is 3.5 s scaled (5 s real at the menu's 0.7 slow-mo) because the 1.6 s FistPump needs to play out after the ~1.5 s beat to the net; the task's '2-3 s' is met for the beat itself, not the hold.",
  "Two Goalkeeper 'X8 dead constants' comments aside, nothing in Goalkeeper/Crosser/Celebration was touched; if Goalkeeper's DeadBand or reaction constants change, the scene's class-summary arithmetic must be re-run."
 ]
}